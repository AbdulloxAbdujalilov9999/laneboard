/* =====================================================================
   Shared vocab — kept identical to dispatch-bro's own lists (index.html,
   `LOAD_STATUSES` / `EQUIPMENT`) so filters and status badges line up
   with the real data coming back from the API.
   ===================================================================== */
const LOAD_STATUSES = ["BOOKED", "DISPATCHED", "IN_TRANSIT", "DELIVERED", "INVOICED", "CANCELLED"];
const EQUIPMENT = ["Dry Van", "Reefer", "Flatbed", "Step Deck", "Power Only", "Box Truck", "Other"];
